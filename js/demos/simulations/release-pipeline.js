import { basicScene, ground, box, cylinder, sphere, label, line, material, createAgent, createDesk, createServerRack, animateAlong, pulse } from '../../core/scene.js';

const $=(root,s)=>root.querySelector(s);
const $$=(root,s)=>[...root.querySelectorAll(s)];
const wait=ms=>new Promise(r=>setTimeout(r,ms));
function domController(stage, reset, cleanup, engine='INTERACTIVE SIMULATION') { return { dispose(){cleanup?.();stage.replaceChildren();}, reset, getStats(){return {engine,fps:'DOM',scene:`${stage.querySelectorAll('*').length} UI elements`};} }; }

export function createReleasePipeline({stage,toast}){
  const phases=[{name:'Feature branch',detail:'feat/recruiter-evidence',icon:'⑂'},{name:'Unit tests',detail:'42 checks',icon:'✓'},{name:'Pull request',detail:'PR #18',icon:'↗'},{name:'Review',detail:'1 approval required',icon:'◎'},{name:'Staging',detail:'preview environment',icon:'S'},{name:'Production',detail:'GitHub Pages',icon:'P'}];
  stage.innerHTML=`<section class="release-product"><header><div><span>DELIVERY CONTROL</span><strong>Git Release Pipeline</strong></div><div><button data-pipeline="run">Run pipeline</button><button data-pipeline="rollback" disabled>Rollback</button></div></header><div class="git-graph"><svg viewBox="0 0 1000 250" preserveAspectRatio="none"><path d="M40 125 C170 125 170 45 300 45 S440 125 560 125 S720 205 840 205 S910 125 970 125"/><path class="main-line" d="M40 125 H970"/></svg>${phases.map((p,i)=>`<article data-phase="${i}" style="--x:${6+i*17}%"><i>${p.icon}</i><strong>${p.name}</strong><small>${p.detail}</small><b>WAITING</b></article>`).join('')}</div><div class="pipeline-bottom"><section><h3>CHECK LOG</h3><pre data-pipeline-log>$ Ready to run release pipeline.</pre></section><aside><h3>RELEASE GATES</h3><label><input type="checkbox" data-gate="tests" checked> All tests passing</label><label><input type="checkbox" data-gate="review" checked> Reviewer approval</label><label><input type="checkbox" data-gate="acceptance" checked> Acceptance proof</label><div class="release-state"><span>PRODUCTION</span><strong data-release-state>v2.4.0</strong><small data-release-health>Healthy</small></div></aside></div></section>`;
  let running=false,deployed=false;
  async function run(){if(running)return;running=true;const phasesEl=$$ (stage,'[data-phase]');phasesEl.forEach(p=>{p.className='';p.querySelector('b').textContent='WAITING';});const log=$(stage,'[data-pipeline-log]');log.textContent='';for(let i=0;i<phasesEl.length;i++){const p=phasesEl[i];p.classList.add('active');p.querySelector('b').textContent='RUNNING';log.textContent+=`\n> ${phases[i].name}: started`;await wait(650);if(i===1&&!$(stage,'[data-gate="tests"]').checked){p.classList.add('failed');p.querySelector('b').textContent='FAILED';log.textContent+='\n✗ Unit test failure: evidence card missing aria-label';running=false;toast('Pipeline stopped at tests');return;}if(i===3&&!$(stage,'[data-gate="review"]').checked){p.classList.add('failed');p.querySelector('b').textContent='BLOCKED';log.textContent+='\n✗ Review approval is missing';running=false;return;}p.classList.remove('active');p.classList.add('passed');p.querySelector('b').textContent='PASSED';log.textContent+=' ✓';}deployed=true;$(stage,'[data-release-state]').textContent='v2.5.0';$(stage,'[data-release-health]').textContent='Healthy · 100%';$(stage,'[data-pipeline="rollback"]').disabled=false;toast('Release deployed to production');running=false;}
  $(stage,'[data-pipeline="run"]').addEventListener('click',run);$(stage,'[data-pipeline="rollback"]').addEventListener('click',()=>{if(!deployed)return;deployed=false;$(stage,'[data-release-state]').textContent='v2.4.0';$(stage,'[data-release-health]').textContent='Rollback complete';$(stage,'[data-pipeline-log]').textContent+='\n> Rolled production back to v2.4.0';toast('Rollback completed');});
  return domController(stage,()=>{running=false;deployed=false;$$ (stage,'[data-phase]').forEach(p=>{p.className='';p.querySelector('b').textContent='WAITING';});$(stage,'[data-pipeline-log]').textContent='$ Ready to run release pipeline.';$(stage,'[data-release-state]').textContent='v2.4.0';$(stage,'[data-release-health]').textContent='Healthy';},null,'GIT DELIVERY PIPELINE');
}
